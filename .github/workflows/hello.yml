name: My First GitHub Action

on: push

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a greeting
        run: echo "Hello from GitHub Actions! This ran automatically."

      - name: Show current date
        run: date
